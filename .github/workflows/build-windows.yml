name: Build Windows

on:
  push:
    branches:
      - main
      - 'feature/**'
  pull_request:
    branches:
      - main

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.6.0'
          modules: 'qtpositioning'

      - name: Install dependencies with vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git
          .\vcpkg\bootstrap-vcpkg.bat
          .\vcpkg\vcpkg install curl nlohmann-json --triplet=x64-windows

      - name: Configure CMake
        run: |
          cmake -B build -G "Visual Studio 17 2022" `
            -DCMAKE_TOOLCHAIN_FILE="${pwd}\vcpkg\scripts\buildsystems\vcpkg.cmake" `
            -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      - name: Deploy Qt dependencies
        run: |
          cd build/Release
          windeployqt6 flood_monitor.exe

      - name: Copy vcpkg DLLs
        run: |
          Copy-Item "vcpkg\installed\x64-windows\bin\*.dll" -Destination "build\Release"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: flood_monitor-windows
          path: build/Release/flood_monitor.exe